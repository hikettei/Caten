(in-package :caten/ajit)

(defstruct (ScheduledGroup))

(defun .intersect (a b)
  
  )

(defun schedule (avm)
  (declare (type avm avm))
  

  )

;; (print (with-no-grad (caten (!sum (!matmul (make-tensor `(3 4) :id 'x) (make-tensor `(4 3) :id 'y))))))
